package com.inepex.ineForm.server.util.mongo;

import org.junit.Test;

/**
 * Old MongoDB perf test — disabled during Java 17 migration.
 * Uses removed MongoDB legacy API (DB, DBCollection, JSON).
 */
public class MongoPerfTest {

    @Test
    public void test() {
        // Disabled: uses removed MongoDB legacy API
    }

}
