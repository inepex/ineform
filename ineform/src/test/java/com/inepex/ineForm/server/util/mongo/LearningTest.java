package com.inepex.ineForm.server.util.mongo;

import org.junit.Test;

/**
 * Old MongoDB learning test — disabled during Java 17 migration.
 * Uses removed MongoDB legacy API (DB, DBCollection, DBCursor).
 */
public class LearningTest {

    @Test
    public void first() {
        // Disabled: uses removed MongoDB legacy API
    }

}
